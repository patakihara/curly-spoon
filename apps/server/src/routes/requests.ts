/**
 * Book request routes — search indexers, ask for a book, watch it download.
 *
 * Registered from `routes/index.ts` from the phase-6 scaffold onwards so the composition
 * root never has to change again as the handlers land.
 */

import type { FastifyInstance } from 'fastify';

export function registerRequestRoutes(_app: FastifyInstance): void {
  // Handlers land in phase 6.
}
